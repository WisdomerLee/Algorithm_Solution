Breadth First Search
너비 우선 탐색
큐를 이용하여 구현함!!

노드 구현 자체는 같으나 검색하는 순서가 달라짐

root노드가 있으면
root노드에 연결된 node들부터 순차로 다 들러본 뒤에
그 node들 밑에 있는 node들을 순서대로 둘러보게 됨
깊이 우선은 node의 child가 있으면 그 child까지 내려가지만... 너비 우선은 child가 있어도 일단 검색이 시작되는 node와 가장 가까운 거리의 node들을 우선 다 들러본다는 것이 차이


노드를 queue에 넣고... 빼고를 반복


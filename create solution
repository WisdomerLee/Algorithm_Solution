문제 해결을 위해서는??

대개 변수, 함수, 배열, 반복문, 조건문, 문자열 등을 씀
상속, 포인터 등은 거의 쓰지 않음

파이썬 기준 입출력 함수
n = input()
print(n)

input()이 함수는 엔터를 치기 전까지의 내용이 모두 들어감(공백 포함)
print()는 문자를 그대로 출력하게 됨

n = int(input())
print(n+4)
위와 같이 처리하면 정수형으로 받게 됨

입력을 여럿 받아서 처리할 경우?
n = input().split()
print(n)

split()으로 쪼개면 입력된 값을 배열 형태로 받아올 수 있음

a, b = map(int, input().split())
print(a)
print(b)

map으로 처리하면 입력을 각각의 값으로 분리하여 받아올 수 있음
위의 예시는 입력된 값을 int 형태로 바꾸어 처리
물론 2개 말고 여럿으로도 받을 수 있음

입력이 매우 많은 경우??
위의 입출력 함수를 쓰게 되면 시간이 초과되어 알고리즘으로 문제 해결이 어려울 수 있음

import sys

for _ in range(10):
  n = int(sys.stdin.readline())
  print(n)

  위와 같이 sys.stdin.readline()과 같은 함수를 이용하여 입력 값을 처리해야 함...

오버플로우 : 자료형이 담을 수 있는 범위를 초과할 경우..
하지만 python의 경우는 신경쓸 필요 없음 : 자동으로 자료형을 할당하기 때문
